{
  "name": "xiaoruhome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "deploy": "npm run build && gh-pages -d dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "gh-pages": "^6.3.0",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  }
}
